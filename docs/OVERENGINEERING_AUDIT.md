# Over-Engineering Audit

delete custom agent planner/prefab/REPL loop. Replace with direct Codex read-only chat/tool execution. [todoist/agent/*]
delete dormant local HuggingFace backend and FP8 safetensors repair layer. Keep Codex/disabled by default; move local LLM to optional extra only if still needed. [todoist/llm/backends/transformers.py, todoist/llm/prompts.py, todoist/llm/tokenizer.py]
delete `ipykernel` and `nbformat` from default dependencies. Use `uv run --with ipykernel --with nbformat` or a notebook extra. [pyproject.toml]
shrink `torch`, `transformers`, `accelerate`, and `langchain-huggingface` from default dependencies. Move local HF runtime/tests to `local-llm` extra. [pyproject.toml]
delete `matplotlib`. Plot code uses Plotly; remove PyInstaller matplotlib hooks too. [pyproject.toml, packaging/*]
delete duplicate automation runtime/service stacks. Keep one `admin_automations` service and make routes delegate to it. [todoist/web/api_components/automation_runtime.py, todoist/web/services/admin_automations.py, todoist/web/routes/admin_automations.py]
shrink dynamic private wrapper layer around split API components. Directly import/call component functions instead of `_sync_api_globals`, `_COMPONENT_EXPORTS`, `_ORIGINALS`, and generated wrappers. [todoist/web/api.py, todoist/web/api_components/*.py]
shrink route modules calling `_sync_api_globals(globals())` on every request. Import explicit dependencies or pass a small context object once. [todoist/web/routes/*.py]
delete 3.8k-line global stylesheet surface. Keep shared layout/card/form/status primitives only. [frontend/app/globals.css]
delete Experimental Habit Tracker Lab route and three components. Fold into one dashboard card only when needed. [frontend/app/experimental/habits/page.tsx, frontend/app/components/ExperimentalHabitTrackerView.tsx, frontend/app/components/HabitTrackerCard.tsx, frontend/app/components/HabitTrackerPlots.tsx]
delete dashboard-embedded Operations/Admin surface duplicating `/control-panel`. Replace with one link to Control Panel. [frontend/app/components/DashboardView.tsx]
native markdown tooltip stack. Render help text as plain text with `white-space: pre-line`; remove `react-markdown`, `remark-gfm`, and `remark-breaks`. [frontend/app/components/Markdown.tsx, frontend/app/components/InfoTip.tsx, frontend/app/lib/dashboardCopy.ts, frontend/package.json]
shrink duplicate Plotly dynamic import and layout-normalization code. Use `PlotCard` or one shared layout helper for all Plotly cards. [frontend/app/components/DashboardView.tsx, frontend/app/components/PlotCard.tsx]
yagni 300-attempt dashboard retry/progress polling state machine. Use a small fetch-with-retry and normal loading/error state. [frontend/app/lib/dashboardHooks.ts]
stdlib `scipy`. Replace single `scipy.stats.gaussian_kde` use with NumPy/native KDE or simpler histogram smoothing. [todoist/dashboard/_plot_lifespans.py]
delete `sqlalchemy`. No imports across first-party code. [pyproject.toml]
delete `tweepy`. No imports found. [pyproject.toml]
delete `tabulate`. No imports found. [pyproject.toml]
delete `yapf`. Ruff formatting is already wired; no imports or Makefile usage. [pyproject.toml]
delete custom Windows installer orchestrator. Replace with thin `uv run pyinstaller` plus existing WiX/PowerShell scripts. [scripts/build_windows.py]
shrink AI breakdown runner progress/comment/failure-accounting workflow. Keep queue selection, generation, insert, and one status payload. [todoist/automations/llm_breakdown/runner.py, todoist/automations/llm_breakdown/progress.py]
yagni legacy flat multiplier subtree expansion and placeholder task cloning. Delete dead `_expand_flat` support path. [todoist/automations/multiplicate/automation.py]
shrink `Cache`/`LocalStorage` named attribute registry. Replace repeated attributes with a filename map and `cache.storage("name", default_factory)`. [todoist/core/utils.py]
delete cache migration backup machinery for old root-level runtime files after cutoff. Replace with no-op or one documented manual migration. [todoist/core/utils.py]
stdlib `joblib` cache persistence for plain dict/list/set payloads. Replace with `json` for structured caches and `pickle` only where object sets truly remain. [todoist/core/utils.py]
shrink API dataclass compatibility layer with `new_api_kwargs`/missing-required-field filling. Replace with pydantic/dataclass models accepting extra fields once at parse boundary. [todoist/core/types.py, todoist/core/utils.py]
delete LLM usage nested totals by backend/model/operation. Replace with append-only last request plus aggregate counters computed for dashboard. [todoist/llm/usage.py]
shrink Gmail typed-contract wrappers for Google API dicts. Normalize at Gmail boundaries instead of maintaining broad `TypedDict` aliases. [todoist/automations/gmail_tasks/contracts.py, todoist/automations/gmail_tasks/automation.py]
native `scripts/run_make_checks.py` parallel process/log runner. Replace with Makefile parallel targets or `make -j` plus per-target shell redirection. [scripts/run_make_checks.py]

Net removable estimate: 6,100-7,900 lines and 55-60 installed packages/transitive dependencies. Direct dependency candidates: `ipykernel`, `nbformat`, `torch`, `transformers`, `accelerate`, `langchain-huggingface`, `matplotlib`, `scipy`, `sqlalchemy`, `tweepy`, `tabulate`, `yapf`, `react-markdown`, `remark-gfm`, `remark-breaks`.
